import express, { Router } from 'express';

const app = express();


////////////////////////////////////////////////////////////////
// Server configuration
////////////////////////////////////////////////////////////////
const router = Router();

app.use(express.json());
app.use(router);


export default app;